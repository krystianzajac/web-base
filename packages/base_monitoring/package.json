{
  "name": "@web-base/base-monitoring",
  "version": "0.1.0",
  "description": "Sentry, analytics, structured logging for web-base",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run --reporter=verbose",
    "lint": "eslint src --ext .ts,.tsx --max-warnings 0",
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "react": "^18.0.0",
    "@sentry/nextjs": "^8.0.0"
  },
  "devDependencies": {
    "@sentry/nextjs": "^8.0.0",
    "@types/react": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.0",
    "typescript": "^5.7.0",
    "vitest": "^1.6.0",
    "jsdom": "^24.1.0"
  }
}
